node_modules/
*.log
.env
.env.local
.DS_Store
*.lua
!scripts/*.lua
luac.out
*.luac

# Proxy lines are credentials. Keep them out of the repo entirely — on Railway
# the filesystem is rebuilt from git every deploy, so they belong in the
# PROXIES env var anyway.
proxies.txt
