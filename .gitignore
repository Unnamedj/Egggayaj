node_modules/
*.log
.env
.env.local
.DS_Store
*.lua
!scripts/*.lua
luac.out
*.luac

# proxies.txt is intentionally tracked: this repo commits it instead of using
# the PROXIES env var. Only do this in a PRIVATE repo — anyone who can read a
# public one can read this file too, credentials included.
